import PrototypeApp from "../../../App";

// The approved prototype remains the visual shell while each feature is
// replaced with Supabase-backed modules during the next milestones.
export default function OperationsPage() {
  return <PrototypeApp />;
}
